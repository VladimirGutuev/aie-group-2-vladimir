# --- Service ---
HOST=0.0.0.0
PORT=8000

# --- OCR ---
# Comma-separated languages for EasyOCR (en,ru,...)
OCR_LANGS=en,ru
MIN_CONFIDENCE=0.3

# --- Detector (YOLOv8) ---
# Set to true to enable detector stage; otherwise OCR runs on full frame.
USE_DETECTOR=false
DETECTOR_WEIGHTS=yolov8n.pt
